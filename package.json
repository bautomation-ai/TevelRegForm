{
  "name": "tevel-trip-registration",
  "version": "0.1.0",
  "private": true,
  "description": "Trip registration scaffold with Airtable proxy and webhook-ready submit",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}